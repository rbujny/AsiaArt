{% extends 'base.html.twig' %}

{% block title %} Portfolio {% endblock %}

{% block content %}
    {% for i in 1..8 %}
        <div class="photos">
            {% for j in 1..3 %}
                <div class="main-photo">
                    <a href="{{ path('app_item_item', {id: 4}) }}">
                        <img src="{{ asset('img/goat.jpeg') }}">
                        <p>GOAT</p>
                    </a>
                </div>
            {% endfor %}
        </div>
    {% endfor %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/cScript.js') }}"></script>
{% endblock %}
